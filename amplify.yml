version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g yarn
        - yarn
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
